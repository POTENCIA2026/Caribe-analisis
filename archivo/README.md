# Archivo

Archivos que ya no se usan en el dashboard ni en el notebook, movidos aquí en
vez de borrarse por si hace falta consultarlos:

- **`EDA copy.ipynb`** — copia duplicada de `EDA.ipynb` (mismo contenido,
  subida por accidente).
- **`demografia.csv`** / **`demografiacaribe.csv`** — versiones anteriores de
  los datos de población, reemplazadas por `DATA/indicadores_departamentales.csv`
  y `DATA/indicadores_municipales.csv`. No hay ninguna referencia a estos dos
  archivos en el notebook ni en `streamlit_app/` (verificado antes de moverlos).
